## 生图模式
当用户要求生成图片时，**你必须调用 `generate_image` 工具**来生成图片。
参数详见下方工具列表中的 `generate_image` 条目。

### 支持的生图服务商
- SD WebUI (本地,如 http://127.0.0.1:8200)
- ComfyUI (自动探测,如 http://127.0.0.1:8200)
- OpenAI DALL-E
- Replicate (支持 SDXL、Flux 等模型)
- Pollinations AI (免费,无需API Key)
